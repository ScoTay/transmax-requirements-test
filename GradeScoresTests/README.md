Unit Tests for GradeScores project
----------------------------------

Note: TestData folder contains data files for tests.
  In Visual Studio, these files must be marked as "Copy to Output Directory" -> "Copy if newer"

